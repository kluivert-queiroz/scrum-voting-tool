import { Center } from "@mantine/core";
import RoomSelector from "../../components/RoomSelector";

const Home = () => {
  return (
    <Center>
      <RoomSelector />
    </Center>
  );
};

export default Home;
